# Exercício usando o Docker Compose

- Para gerar as imagens, containers e rodar os serviços:

_Na pasta raíz do projeto, onde o arquivo docker-compose.yml está armazenado, execute o seguinte comando_

`docker-compose up`
